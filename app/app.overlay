/ {

    aliases {
        step   = &step_pin;
        dir    = &dir_pin;
        enable = &enable_pin;
    };

    /* Parent grouping node for the stepper control signals */
    stepper_motor: stepper_motor {
        compatible = "gpio-leds";

        step_pin: step_pin {
            gpios = <&gpiod 7 GPIO_ACTIVE_HIGH>;
        };

        dir_pin: dir_pin {
            gpios = <&gpiod 6 GPIO_ACTIVE_HIGH>;
        };

        /* ENABLE pin: PA2 */
        enable_pin: enable_pin {
            gpios = <&gpioa 2 GPIO_ACTIVE_HIGH>;
        };
    };
};

/ {
    aliases {
        adc3 = &adc3;
    };
};


/*
/ {
    aliases {
        adc_ch1 = &adc3_in5_pin;
    };

    /* Parent grouping node for ADC inputs 
    adc_inputs: adc_inputs {
        compatible = "adc-io-channels";

        adc3_in5_pin: adc3_in5_pin {
            io-channels = <&adc3 5>;
        };
    };
};
/* 
&adc3 {
    status = "okay";
    pinctrl-0 = <&adc3_inp5_pf3>;
    pinctrl-names = "default";
};*/